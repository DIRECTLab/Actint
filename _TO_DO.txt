To Do:

backend/mcp_servers/adsb: Do we want to implement Ryan's condensed tools or just merge it and have the option to use either tool set? 
backend/tests: Do we want to consolidate all testing scripts here? postgres and sqlite connection tests 
data/db/* : what are the other .db files can we delete them

work through front end


Done:

backend/data_collection: change get_conn to use the correction DB helper function in all the scripts here
backend/mcp_servers/utils/distance_calculation: add my RDP algorithm implementation and other helpers 
backend/data processing: Templeton do you still use this to import AIS data or do you have another method now? DELETED
backend/benchmarking: See if we want to keep this, has the benchmarking branch been merged, do we have the latest and greatest? 
data/db/chroma: remove this folder

backend/transport: figure out what simulation_events(deprecated) does and it we want to keep it: doesn't work as we don't have helper functions and I can't find them in the git history